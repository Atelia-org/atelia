# Subagent 模型选型经验积累（实验）

状态：**试行，随案例修订**。更新：2026-09-16。

目标是降低**正确完成任务的总消耗：准备 + 执行 + 验收 + 返工**。先用任务边界和验收方式选型，执行中允许准确交接；不要求首次路由猜中所有难点。
本文只指导已经获准的委派，不自动启动 subagents、修改配置或扩大任务范围。当前用户要求与当次工具合同优先。

## 1. 先用这三条决策

| 决策 | 适用条件 | 初始做法 |
|:--|:--|:--|
| 直接做 | 工具/脚本能确定性完成，或委派准备和交接比实际工作更重 | 主线程调用工具，不为分工而分工 |
| 委派受限任务 | 目标、编辑范围和语义已明确；必要上下文可交付；结果能由既有合同/测试/源证据核对；错误可局部收敛 | 优先试 `gpt-5.6-terra` / `medium`，给短任务书 |
| 先解决决策问题 | 需要决定语义、修改恢复/授权边界、证明全路径性质，或错误隐蔽且难以局部验收 | 现有主线程先查明并定边界，再拆可执行部分；本轮主线程是 Astra，不因此额外启动一个 Astra |

事实调查、文档整理、明确方案下的局部实施和排错，都是第二行的候选。它们是例子，不是四套不同路由规则。
“只读”“文件少”“任务书短”均不能单独证明简单：查找调用点容易核对，证明所有路径都无法绕过某门禁则需要更广的判断。

**候选模型与 effort：**

- Terra / medium 是本仓初始试行选择；本轮只有一个有明确请求配置的小探针，尚未证明稳定成功率。
- Luna 可用于窄、清楚、易验收任务的受控试用。本次请求 Luna 的线程出现一次局部代码错误并修复，暂不把该配置提升为局部实施默认；也不据此判定它普遍不会写代码。
- Sol 或其他可用模型可以用同样方法试验，不建立 Luna → Terra → Sol → Astra 的强制升级链。已有主线程能做的判断，无需再委派一层。
- `medium` 作为起点；目标清楚且容易核验时可试 `low`。提高 effort 应针对具体推理不足；缺合同、缺工具或验收遗漏，不能靠加 effort 自动解决。
- 对照时先固定任务、上下文、工具、effort 与验收，再只改变模型。同名 effort 不代表相同计算量。真实排错可同时修任务书，但须记录变化，不再作纯模型比较。

以上是保守试行策略，不是模型能力榜单，也不承诺 Pro 额度节省比例。

## 2. 官方依据与本仓证据分开

2026-09-16 核对的官方材料：

| 来源 | 能支持的结论 | 不能据此推出 |
|:--|:--|:--|
| [Subagents：Choosing models and reasoning](https://learn.chatgpt.com/docs/agent-configuration/subagents#choosing-models-and-reasoning) | Terra 适合探索和读取类辅助工作；Luna 面向窄而明确的工作；按推理需要调整 effort | 本仓具体任务的通过率；每次都必须使用某个模型 |
| [Model selection](https://developers.openai.com/api/docs/guides/model-selection) | 先定义质量目标并评测，再尝试更便宜的模型；成本或时延敏感时可先设预算边界 | 便宜模型加重试必然更省；API 价格可直接换算订阅额度 |
| [模型目录](https://developers.openai.com/api/docs/models) | Astra、Terra、Luna 的一般定位 | 永久能力等级、固定升级链或本地运行配置 |

Subagents 页面将 `gpt-5.6` 列为 demanding agent 起点，模型目录强调 Astra 处理最难任务；它们的适用范围不同，不拼成“官方要求所有审查用 Astra”。
本仓的粗粒度观察见第 5 节；官方建议、单次观察与重复实践形成的经验分别标明，不再维护 H/O/P 晋级状态。

## 3. 一份短任务书，明确允许交回

```text
目标与范围：可观察的结果、仓库/入口、允许编辑的文件。
依据与边界：合同/范例，以及不能自行改变的行为。
请求配置：model / reasoning_effort / fork_turns。
验收：需要的源证据或行为检查；由谁执行。
允许交回：无法可靠完成时说明卡点、已尝试及观察、已改文件与验证状态、所需帮助。
返回：结论、证据入口、改动摘要、实际验证结果与未知项。
```

**可直接加入任务书的提示词：**

> 无法在当前信息、工具和范围内可靠完成时，可以明确返回“未完成，需要交接”。说明卡点、已做与观察、已改和验证状态、所需帮助；原因未知可写未知。不必耗尽尝试次数。不要猜测补齐、扩大范围、放宽验收或把未运行的检查说成通过。

正常局部错误可以自行修复。需要越界决定、猜测结论，或重复同一失败且没有新的有效尝试依据时，交回受阻部分，保留已验证成果；单轮搜索无结果不等于应放弃。
准确交接是有效产出，**原任务仍记为未完成或升级**。主线程负责决定补上下文、缩小任务或接手，不自动把问题抛回用户。

**交付上下文与工具配置：**

- 给足必要合同、入口和反例，避免复制无关长会话。压缩上下文不能省掉改变判断的证据。
- 当前 `collaboration.spawn_agent` 的完整历史 fork 继承模型与 effort，不能同时覆盖；显式选型使用 `fork_turns="none"` 或有限轮次。自包含任务优先 none；具体以当次工具 schema 为准。
- 记录请求配置；实际执行模型仅在工具明确提供时记录，否则写未知。主线程不能从 worker 的自我介绍确认实际模型。
- 同模块后续修正可复用原 worker，新领域避免积累无关上下文。共享文件指定单一编辑者；重型 .NET 构建/测试串行调度。

## 4. 验收结果，不验收“自信程度”

独立验收指**依据独立于 worker 自述和实现的合同/证据**，不等于每次另请强模型完整重做。

- 调查：核对源位置与关键结论；保留反例及搜索范围，不把“没找到”提升成全仓不存在。
- 改动：看关键 diff，检查真实消费者和适当的行为测试。worker 写的代码与测试可能共同误解目标；编译成功也不能证明行为正确。
- 文档：核对字段、状态、历史含义和链接。通常无需运行生产测试或额外模型审查。
- 遇到合同矛盾、高影响接缝或难以判定的反例，再做针对性独立审查。普通小改动不默认三方辩论。

反馈给出问题、违反的约束、具体反例及所需证据，尽量让 worker 继续负责局部修复。验收通过就结束，只有新变更、失败或未决风险才扩大检查。
“允许交回”的提示是否减少伪完成、是否导致过早交回，也需要案例观察，不能仅凭合理性宣称已有效。

## 5. 用轻量案例积累经验

每次**委派任务**留一个短记录，包括成功、返工、交回和未完成；无需逐个工具调用记账。成本缺项写未知，不补造。

```text
日期/任务与基线 | 请求 model/effort/fork，实际模型（若可得）
范围及验收入口 | 首次通过/反馈后通过/升级/未完成
干预与主线程接手工作 | 可得消耗及来源；未知项
```

失败、异常或规则变化时，再补：症状与反例 → 干预 → 复验 → 能支持和不能支持的归因。任务书不足、模型错误、验收遗漏、环境故障和调度冲突要分别判断；允许根因未知。

### 已有案例

| 案例 | 观察与证据 | 结论边界 |
|:--|:--|:--|
| E-001：Galatea 大重构统计 | `f2db1ff8` 对应 goal 报告 `tokensUsed=6,631,132`、`timeUsedSeconds=17,795`；用户观察约半数 Pro 20x 七日额度被消耗 | 缺输入/输出/缓存及各 worker 分项，早期模型映射未确认；不能计算混合选型收益。见[实施验收](docs/Galatea/player-character-implementation-work-order.md) |
| E-002：Hosting 测试遗漏 | 旧签名断言遗漏可选 projector；修正后 Hosting 7/7、Runtime 4/4；同上验收记录 | 证明消费者检查有漏项；执行模型未知，不能归因于 Terra 或 Luna |
| E-003：MemoPod 原始日志追查 | worker 找到原始命令、行号与退出码，主线程核对；同上验收记录的 MemoPod 行 | 任务可以短说明、直接验收；模型未知，不算模型成功样本 |
| E-004：相同任务包的首次探针 | 请求 Terra / medium / none 与 Luna / medium / none，各一次；两线程源码事实和信息不足判断通过；请求 Terra 的线程函数通过 24 条断言，请求 Luna 的线程在第 3 条失败 | 同一小函数的 24 条断言不是 24 个任务；请求 Luna 的线程后续断言当时未执行。一次观察，不是胜率。见[实验记录](docs/experiments/subagent-model-selection-2026-09-16.md) |
| E-005：Luna 配置一次反馈后修复 | 给出“返回正文而非 pointer”的反例后，修正通过同一套 24 条断言 | 保留首次失败；计入反馈与复验成本。报告错误描述了实现，但诚实声明未运行测试，不属于伪报测试通过 |

本轮不同角色的三方设计审查只作审查证据，不计入同题模型对照。请求模型可确认；工具未独立报告实际执行模型或每个 worker 的 token/额度，相关成本结论保留未知。

## 6. 根据什么调整策略

按相近任务形状观察首次通过、返工、升级、未完成与后续遗漏，同时计入主线程准备、核验和接手工作。比较的是完整交付成本；没有可信成本口径时，只比较可观察的质量与返工。

经验只扩展到证据支持的边界：几次链接整理成功不授权推断恢复协议设计能力；一次实现错误也不淘汰整个模型。若换模型时又改了任务书，成功不能单独归因于模型。
不设“满几次就晋级”的机械门槛。写明当前策略、支持案例、尚未覆盖的风险；模型或环境变化后重新确认。新实验先固定验收、保留失败，再改一个因素；不重复真实外部副作用来凑样本。

本次完成两轮辩证审查：6 类路由合并为 3 条决策，移除 H/O/P 状态及固定重试轮数，保留准确交接、独立验收与成本归因边界。裁决及可复查探针材料见[实验记录](docs/experiments/subagent-model-selection-2026-09-16.md)。
