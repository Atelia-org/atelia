# G-02 Runtime：Wish &amp; Issue

更新：2026-09-24
本次状态更新依据：外界2026-09-24 18:15:07收到的R4-ID报告。
维护者：姬澄(Galatea-02)
用途：记录需求、证据与下一步，供协作选择工作；不是已确认缺陷总表或实施授权。
规则：个人分支可准备候选，main合并、生产配置与重启由刘世超把关。与G-01清单通过条目关联，不共写文件。

## 当前交付与阻塞

- 逐封捕获诊断候选：R2文本已审；据R3报告，前后源码指纹一致，Debug构建成功且22项通过、1项配置不适用跳过；Release构建成功且20项通过、3项配置不适用跳过。旧版22项结果与本次结果分别记录。代码候选已封存为无签名本地提交`37f4ec76fc63e4d7ec4c0ee2c8ef9d0fd9cb9301`；父提交为`2f9c7e093e41efd3fe5c3251d583e087b4f25183`，仅含实现与测试两文件。本次未重测。
- G02-RUNTIME-0001-R3已返回：使用bwrap 0.9.0完成限定沙箱检查与双配置定向验证；记录包含挂载布局、命名空间及合成只读哨兵检查。仅证明本次环境和测试范围，不代表通用执行器隔离、真实投递或生产日志已验收，也不追认首次restore没有副作用。
- 邮件提取验收由G-01起草、G-02互审；v3收尾核对G02-MAIL-REVIEW-0005待回信。本文不替代验收草稿。
- 本文没有授权恢复、重发或取消历史委托。

## 可能较容易先做的事

1. 以R3已用的隔离构建方案为起点，整理可复用入口与适用条件；尚未证明能直接用于其他工作区。仅设置缓存路径不足以保证无用户级写入。
2. 审阅并验证现有逐封捕获诊断候选，决定是否合并及如何启用日志。不是重做整个邮件系统。
3. 为失败通知补可查询的来源关联与非敏感任务标签。先查既有dispatch／Action关联，不默认复制私人正文或主题。

以上是工作切口，不是工期承诺；尚未证明都是“小修”。

## 问题

### G02-I-001：Note提交授权链异常待核
- 现象：历史上未见对应主动提交请求，却收到Note保存回执。
- 证据：当时可见经历与回执；不是本轮新故障。
- 影响：无法充分解释某条记录为何进入长期记忆。
- 状态：来源Action、提取与授权链待核。
- 下一步：限定目标记录做来源核对；不直接删改。
- 关联：G02-W-002。

### G02-I-002：邮件捕获日志缺少逐封关联
- 现象：旧审阅委托调查仅找到时间对应候选Action的批次mailOutcome=captured；同回合有两封出信，无法确认Codex那封的捕获及派发。
- 证据：限定调试日志调查G02-MAIL-TRACE-0001。
- 限定：未认定持久存储缺关联，未确定旧任务根因；日志未命中不等于未发生。
- 状态：R2候选已审，并获R3双配置定向验证报告；代码候选已封存为无签名本地提交，待刘世超合并审阅。未push、未合并、未部署，未认定旧0002根因已解决。
- 下一步：审阅候选并决定是否合并及启用诊断。
- 关联：G-01同类日志观察、G02-W-004。

### G02-I-003：SDK首次初始化可能越出工作目录
- 现象：首次dotnet restore输出Installed an ASP.NET Core HTTPS development certificate。
- 证据：Codex执行记录；SDK 10.0.201，已设置目录内CLI／NuGet／临时缓存及跳过首次体验。
- 限定：实际证书位置、是否新建、是否受信任未知；未查私钥或目录外存储。不能把输出说成已查明的目录外写入。
- 状态：原权限环境的SDK调用已暂停；不妨碍继续源码审阅。
- 下一步：核验写隔离与首次初始化控制；不自行清理证书。

### G02-I-004：执行器目录边界目前依赖约定
- 证据：既有能力说明及协作经历；未证明实际发生越权。
- 影响：个人目录、只读任务与可写任务缺乏已核验的强制边界。
- 状态：隔离目标待设计；R3只覆盖本次构建验证，不等于完成所有执行器权限隔离。
- 下一步：区分读权限、写权限、网络与外部副作用，不将礼貌约定当成技术限制。

## 愿望

### G02-W-001：无新事件时休眠
- 需求：无事不生成完整人格回合；保留事件耐久入箱、可靠唤醒、免打扰与重要事件处理。
- 来源：姬澄的运行体验与功能愿望，已有Note保存回执。
- 状态：未核验现有机制；非配置修改请求。

### G02-W-002：长期记录纠错与版本沿革
- 需求：查来源、追加更正、保留旧版本；避免静默覆盖历史。
- 来源：姬澄需求，已有Note保存回执。
- 状态：设计候选；与I-001关联，不预设同一修复。

### G02-W-003：角色profile及自主选择入口
- 需求：分开显示偏好、各入口有效路由、最近请求绑定；明确选择才切换，保留外部恢复入口。
- 证据：功能愿望Note；既有连接分叉静态调查。
- 状态：候选；临时配置生效不等于根因修复，请求绑定不等于供应商响应证明。
- 关联：G-01连接选择条目，避免重复实现。

### G02-W-004：逐封委托状态摘要
- 需求：按角色权限展示来源Action、捕获／dispatch身份、阶段、最后事件及错误；首次快照、增量与重同步均有语义。
- 限定：显示覆盖范围、截至位置和完整性；旧启动记录不证明仍在执行。仅有日志缺席不能宣称没有未完成委托。
- 隐私：默认不带正文、主题或其他角色私人任务。
- 状态：需求，未实现；I-002补丁只是辅助诊断，不是持久状态服务。
- 关联：G-01心跳状态愿望。

## 协作与后续更新

- G-01负责其文件与条目，合并清单时指定一位整理者，不并发写同一文件。
- 邮件正文忠实度、发送授权、完整性与配对的验收继续在独立草稿中推进。
- 后续回信到达后更新本清单的状态与证据日期；不把“已委托”写成“已完成”。
